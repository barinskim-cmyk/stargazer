# Stargazer — Бэклог

---

## ✅ Готово (последние сессии)

### Небо — IAU созвездия
- [x] Подключить `star-renderer` к реальным созвездиям (LOD3 через `drawStarPointToCol`)
- [x] Тёплый оттенок IAU-звёзд: `IAU_GLOW = '205,218,255'`, `IAU_CORE = '242,244,255'`

### Цветные звёзды
- [x] Модуль `star-colors.js` — палитра из 5 цветов (gold, white, blue, rose, purple)
- [x] `drawStarPointToCol` / `drawStarPointCol` — меняют только ореол (L1), спайки и ядро всегда золотые
- [x] Выбор цвета созвездия в экране «Моё созвездие» перед публикацией
- [x] Цвет сохраняется в Supabase (`data.color`) и рендерится в небе (LOD2 + LOD3)
- [x] Настройки → Игра: выбор цвета звёзд по умолчанию (`_appSettings.starColor`)
- [x] `draw()`: игровые звёзды рисуются выбранным цветом (активная игра + конец игры)
- [x] Экран победы (`victory-duel.js`): мини-созвездия рендерятся выбранным цветом
- [x] i18n: ключи `settings_star_color_title/sub` для EN / RU / ES / ZH / PT

### Баги
- [x] iOS: свотчи без заливки → `style.backgroundColor` вместо `style.background`
- [x] iOS: кнопки-свотчи невидимы → `-webkit-appearance: none` в CSS
- [x] Небо пустое (нет созвездий) → `IAU_CORE` не была объявлена → `ReferenceError` в `_skyDraw`

---

## 🔜 В работе / следующие задачи

### Небо
- [ ] Тап на созвездие в небе → открывать карточку с цветным превью (сейчас цвет не отображается в detail-превью)
- [ ] «Мои созвездия» — фильтр/поиск по цвету
- [ ] Анимация появления нового созвездия в небе (fade-in уже есть, добавить «вспышку»)

### Цветные звёзды
- [ ] Цвет созвездия виден в списке сохранённых созвездий (миниатюра с цветным ореолом)
- [ ] Возможность изменить цвет уже опубликованного созвездия (редактировать и перепубликовать)

### Геймплей
- [ ] Режим «Дуэль с другом» по локальной сети / share-ссылке
- [ ] Подсказки уровня сложности для новичков (опция в настройках)

---

## 🕳 Новый режим: Поддавки + Чёрные дыры

### Концепция
Альтернативный режим игры с инвертированными правилами — **проигрывает тот, кто завершит созвездие** (misère).
Визуальная тема — минимализм с анимированными «чёрными дырами»: тёмные вихревые объекты вместо золотых звёзд, гравитационные линзы вместо ореолов, тёмные соединительные линии.

### 1. Выбор режима — `index.html` / `home-screen.js`
- [ ] Добавить кнопку «Поддавки» на экран выбора режима рядом с «vs AI» / «vs Player»
- [ ] Флаг `isPoddavki = false` в состоянии игры; передаётся в `startGame()`
- [ ] Показывать иконку/лейбл режима на игровом поле (напоминалка)

### 2. Игровая логика — `game-engine.js`
- [ ] `endGame()`: если `isPoddavki`, **победитель = тот, кто НЕ сделал последний ход** (т.е. `1 - currentPlayer` в момент завершения созвездия)
- [ ] `moveWouldScore()` — в поддавках «опасный» ход = тот, который приносит очко (сейчас это «хороший» ход); нужен флаг для AI и для подсветки
- [ ] `blockingPoint` — в поддавках логика инвертируется: «блокирующая» точка = та, через которую тебе НЕЛЬЗЯ ходить (завершишь = проиграешь)

### 3. Искусственный интеллект — `stargazer-ai.js`
- [ ] Новая стратегия `makeAIMovePoddavki()`: AI **избегает** ходов, завершающих созвездие
- [ ] AI ищет ходы, которые вынуждают оппонента делать «убийственный» последний ход (форсированные поддавки)
- [ ] Уровень сложности работает аналогично: низкий уровень = иногда случайно «проваливается» и завершает; высокий = почти никогда

### 4. Визуал: тема «Чёрная дыра» — `star-renderer.js` / `themes.js`
- [ ] Новая функция `drawBlackHole(ctx, px, py, degree, ...)`:
  - Центр — тёмный круг (`rgba(5,5,15,1)`)
  - Вокруг — тонкое кольцо гравитационной линзы (светлый тонкий ореол `rgba(180,180,220,0.4)`)
  - Вместо «лучей» (спайков) — слабые «дуги» изгиба пространства (arc-based, не линейные)
  - Анимация: медленное вращение дуг (через `performance.now()`)
- [ ] Новая палитра темы: фон — глубокий тёмно-синий (`#04050f`), линии — `rgba(140,140,180,0.6)`, нет светлых ореолов
- [ ] Тема регистрируется в `THEMES` как `'blackhole'`, базируется на `minimal` но с переопределёнными ключами
- [ ] В режиме поддавки тема `blackhole` применяется автоматически при старте игры (с возможностью сменить вручную в настройках)

### 5. Линии и рендер — `line-renderer.js` / `draw()`
- [ ] Линии в режиме поддавки: тонкие, полупрозрачные, немного «размытые» (тема чёрной дыры — нет жёстких линий)
- [ ] Предпросмотр линии (preview): вместо обычного цвета — «предупреждающий» оттенок если ход завершит созвездие

### 6. Экран победы — `victory-duel.js` / `victory-handlers.js`
- [ ] Другой текст победы: «Звезда поглощена» / «Ты избежал бездны»
- [ ] Анимация победы: не «взрыв звезды», а «схлопывание» — reverse-анимация коллапса
- [ ] `victory-duel.js`: рендерить итоговое созвездие в стиле чёрной дыры

### 7. Рейтинг и прогресс — `win-system.js`
- [ ] Победы в поддавках считаются отдельно или в общий рейтинг? Решить и зафиксировать
- [ ] Достижения: «Выжил в 5 поддавках подряд», «Заставил AI провалиться»

### 8. Небо — `sky-screen.js`
- [ ] Решить: публикуются ли «поддавочные» созвездия в небо?
- [ ] Если да — отдельная секция «Чёрные дыры» или метка на созвездии в небе

### 9. Интернационализация — `i18n.js`
- [ ] `mode_poddavki`, `mode_poddavki_sub` — «Поддавки» / «Проигрывает тот, кто завершит»
- [ ] `victory_poddavki_win`, `victory_poddavki_lose` — тексты на экране победы
- [ ] `blackhole_theme_name` — название темы для всех 5 языков

### Профиль
- [ ] Страница профиля другого игрока (тап на имя в небе → профиль)
- [ ] Счётчик опубликованных созвездий в профиле

---

## После рефакторинга: Архитектура конфигурации

### Идея

Разделить всё на три слоя:

1. **Неизменяемые модули** (engines/systems) — логика, которую пользователь не меняет: game-engine, stargazer-ai, sky-screen, star-renderer, line-renderer, victory-renderer и т.д.

2. **Пользовательские настройки** — то, что пользователь может настроить под себя: выбранная тема, язык, звук вкл/выкл, уровень подсказок, предпочтения отображения.

3. **Данные пользователя** — то, что настраивается под конкретного пользователя автоматически: прогресс (ранг, звёзды, история матчей), купленные темы/предметы, сохранённые созвездия, статистика, достижения.

### Как это должно работать

- Слои 2 и 3 хранятся в отдельных файлах конфигурации (JSON/localStorage + Supabase sync).
- При загрузке приложения модули из слоя 1 подгружают нужные данные из слоёв 2 и 3 в зависимости от сценария (онлайн/оффлайн, новый/вернувшийся пользователь, гость/авторизованный).
- Смена темы, языка или любого оформления мгновенно применяется ко всем модулям, включая Sky Screen.
- Чистое разделение: модули не хранят состояние — они получают его из конфигов.

### Задачи

- [ ] Спроектировать формат файлов конфигурации (user-settings.json, user-progress.json)
- [ ] Определить точный список пользовательских настроек (слой 2)
- [ ] Определить точный список данных пользователя (слой 3)
- [ ] Создать менеджер конфигурации (config-manager.js) — загрузка, сохранение, синхронизация
- [ ] Рефакторнуть модули чтобы читали состояние из конфигов, а не из глобальных переменных
- [ ] Продумать сценарии: первый запуск, оффлайн, смена устройства, сброс прогресса
